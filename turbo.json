{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "ADMIN_SECRET",
    "TURSO_AUTH_TOKEN",
    "TURSO_DATABASE_URL",
    "ZAPIER_WEBHOOK_URL"
  ],
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
